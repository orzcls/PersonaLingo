# Profile Template — 个人资料输出模板

> Agent 在 Step 4 根据 `corpus.json` 填充本模板，写入 `corpus/<corpus_id>/profile.md`。
> 模板使用 `{{ placeholder }}` 语法；未获得的字段输出 `—`。

---

# {{ learner_profile.background_title }}

> {{ learner_profile.one_sentence_tagline }}

## 1. Snapshot

| 维度 | 值 |
|---|---|
| MBTI | {{ persona.mbti }} |
| Target Band | {{ learner_profile.goal_vector.target_band }} |
| Exam Window | {{ learner_profile.goal_vector.exam_window }} |
| Current Band | {{ learner_profile.goal_vector.current_band }} |
| Primary Style | {{ persona.communication_style.structure_pattern }} |

## 2. Strengths

{{#each learner_profile.strengths}}
- {{ this }}
{{/each}}

## 3. Weakness Signals (with evidence)

{{#each learner_profile.weakness_signals}}
- {{ this }}
{{/each}}

## 4. Anchor Stories

{{#each anchors}}
### {{ id }} · {{ title }} ({{ category }} / {{ emotion_tag }})

> {{ one_liner }}

**Beats**

{{#each story_beats}}
1. {{ this }}
{{/each}}

**Reusable for topics**: {{ reusable_for_topics | join ", " }}

{{/each}}

## 5. Topic Bridges (selected)

| Bridge | Topic | Hook |
|---|---|---|
{{#each bridges}}
| {{ id }} ← {{ anchor_id }} | {{ topic }} | {{ hook_sentence }} |
{{/each}}

## 6. Vocabulary Upgrade

> Band tier target: {{ learner_profile.goal_vector.target_band }}

{{#each vocabulary}}
- **{{ item }}** _(tier {{ band_tier }})_ — {{ example_from_anchor }}
{{/each}}

## 7. Sentence Patterns

{{#each patterns}}
- `[{{ purpose }}]` {{ skeleton }}
{{/each}}

## 8. Communication Style Cheatsheet

- **Opener**：{{ persona.communication_style.opener_pattern }}
- **Structure**：{{ persona.communication_style.structure_pattern }}
- **Closing**：{{ persona.communication_style.closing_pattern }}
- **Avoid**：{{ persona.communication_style.avoid | join "; " }}

---

_Generated by PersonaLingo Install-only Skill · schema v3.1_
